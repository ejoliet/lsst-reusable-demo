interface:
  display_name: "Lsst Reusable Demo"
  short_description: "Repo-specific patterns and workflows for lsst-reusable-demo"
  default_prompt: "Use the lsst-reusable-demo repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true